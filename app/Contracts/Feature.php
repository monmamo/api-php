<?php

namespace App\Contracts;

/**
 * 💢 Should be named "Trait" but we have to name it "Feature" because PHP interprets "Trait" as the "trait" keyword.
 */
interface Feature extends HasSizeDelta {}
